spring:
  data:
    redis:
      host: localhost
      #本地的配置
#      host: redis
      #容器的配置
      password: ${REDIS_PASSWORD:}
      port: 6379
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
